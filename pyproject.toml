[project]
name = "color-picker"
version = "0.1.0"
description = "MacOS Color Picker Widget"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyinstaller>=6.22.0",
    "pywebview>=6.2.1",
]
